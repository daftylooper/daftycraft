#pragma once
#include "Macros.h"

#include "PerlinNoise.h"

class Chunk{
    public:
        unsigned int width;
        unsigned int length;

        //generate terrain for this chunk
        Chunk(unsigned int width, unsigned int length);
        void 
};